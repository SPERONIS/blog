## 前言
最新在团队各个业务中开始落地 Agent，发现在于合作方讨论时存在或多或少的信息 GAP，不可否认现在 AI 行业发展已经达到每周甚至每天都有新的技术或者概念出来，学都学不过来还别说应用了。本文对于 Agent 场景落地过程中涉及到的跟前后端、设计、PD、甚至运营都要对齐的一些信息进行一些思考和总结。
## AI Copilot 和 AI Agent

### 1.1. AI Copilot
需要用户的引导，更多地起辅助作用，依赖清晰明确的提示（prompt）来发挥作用。人类与 AI 的工作量相当，AI 根据人类的指令完成初稿，人类负责设定目标、修改调整并最终确认。

![[Pasted image 20250629111738.png]](https://github.com/SPERONIS/ciza1/blob/main/Pasted%20image%2020250629111738.png?raw=true)
### 1.2. AI Agent
具有较强的自主性，可以根据目标自主思考和行动，无需人类的详细指导。它能够独立完成 任务拆分、工具选择、进度控制，并在实现目标后自主结束工作。
![[Pasted image 20250629111826.png]]
![[Pasted image 20250629111915.png]]
### 1.3. Agentic AI
>《AI Agents vs. Agentic AI: A Conceptual Taxonomy, Applications and Challenges》 https://arxiv.org/pdf/2505.10468

即高度自主决策 AI，主要体现在多 Agent 的高效协作、高级的推理&计划并对任务进行有效 分解、共享的上下文内存、高度的协作机制，更加强调社会行为和分布式智能。
## 1.4. 小结

| 特性         | **AI Copilot**                  | **AI Agent**        |
| ---------- | --------------------------- | --------------- |
| **核心角色**   | 辅助者 (你的智能助⼿)                | 执⾏者 (可独⽴⾏动的智能体) |
| **目标**     | 增强⼈类能⼒，提⾼效率                 | ⾃主完成任务          |
| **⾃主级别**   | 低：依赖⼈类指令和决策                 | ⾼：可⾃主规划、决策、执⾏   |
| **交互⽅式**   | 对话式交互（问答、建议、实时协作）           | 可后台运⾏，⽆需持续交互    |
| **任务范围**   | 单⼀任务 / 即时响应                 | 复杂任务链（多步骤、跨⼯具）  |
| **是否拆解任务** | 通常不主动拆解任务                   | 主动拆解任务为⼦⽬标      |
| **应⽤**     | GitHub Copilot、Aone Copilot | AutoGPT, Manus  |

![[Pasted image 20250629164851.png]]


## 二、 智能体交互
### 2.1. 什么是智能体交互
智能体交互式解决的是如何连接：人 + 智能体 + 计算机，其包含了 Copilot 的交互形态 + Agent 的执行内核。智能体交互涉及单智能体与多智能体之间的协作、人机交互以及环境互 动等多个层面。从风控产品视角来说就是通过友好的交互帮助运营/产研与智能体有更好的协同。

### 2.2. 智能体交互方式
- 常见的 4 种交互方式： CopilotPopup，在页面通过浮层方式弹出交互对话框
- CopilotSidebar，在页面左侧平行位置展示交互对话框
- CopilotChat，沉浸式对话界面 
- 其他如 HeadlessUI、以及 CopilotChat 与 CopilotSidebar 混合的形式。
- Answer 回复区域 
- Analysis 分析结果区域
- Question 提问区域
- DeepAnalysis 深度分析结果区域 
- 除此之外，html 等信息需要快速转化成用户运行环境上下文

## 2.4. 交互协议
工具、Agent、人之前进行交互可通过协议来降低成本、提升扩展性和安全性。目前业界对 工具 + LLM 之间使用 MCP 协议具有一定的共识，Agent + Agent 之间是否需要特定的协议 还有一定的争议，Google 等厂已经设计了 A2A 的协议；而到了 Agent + 人之间的交互协议 则需要使用 AG-UI 协议来进行交互。
![[Pasted image 20250629112310.png]]
##### MCP
MCP（Model Context Protocol，模型上下文协议），由 Anthropic 于 2024 年底提出，旨在 解决 AI 系统与外部工具、数据源集成时的碎片化和高成本问题。
![[Pasted image 20250629112346.png]]

#### A2A
谷歌在 2025 年 4 月 9 日的 Google Cloud Next 25 大会上正式开源 A2A 协议，并联合 50 余家科技企业（如 Salesforce、SAP、埃森哲等）共同推动生态建设。A2A 协议基于 HTTP/JSON-RPC 等通用标准，支持智能体间的能力发现、任务管理和多模态交互，目标是 解决跨平台智能体协作的 “孤岛” 问题。微软在 2025 年 5 月宣布 Azure AI Foundry 和 Copilot Studio 支持 A2A 协议，并与谷歌合作开发扩大该协议的应用范围。
![[Pasted image 20250629112418.png]]

![[Pasted image 20250629112427.png]]
![[Pasted image 20250629112439.png]]

Agent注册：
``` json
{ 
	"capabilities": { "streaming": false }, 
	"defaultInputModes": ["text" ], 
	"defaultOutputModes": [ "text" ], 
	"description": "提供天气相关的查询功能",
	 "name": "天气 Agent", 
	 "skills": [], 
	 "url": "http://aa.com:1000", 
	 "version": "1.0.0"
	}
```

请求体中的 message：
``` JSON
{ 
	"contextId": "ssdasdas-dasd-sdasdasd-3qdq-45h3rthey5", 
	"kind": "message", 
	"messageId": "rh3renfb-2rqdasasdg-5hrtytu-j6urejhs", 
	"parts": [ 
		{ 
			"kind": "text", 
			"text": "杭州今天的天气怎么样？"
		}
	],
	"role": "user" 
}
```

返回结果中的 Artifacts（请求 Agent 返回的产物）:
``` JSON
[ 
	{ 
		"artifactId": "dasd1-g24gwevdf-45yrwgreu-6i6rjtyerg", 
		"description": "", 
		"name": "天气查询结果", 
		"parts": [{ 
			"kind": "text", 
			"text": "今天杭州天气晴朗，平均气温 26 摄氏度"
		}]
	}
]
```

返回结果中的 Task（被调用 Agent 根据请求所生成的任务）:
``` JSON
{ 
	"artifacts": [{...}],
	"contenxtId": "313rt320det20-4grbwtyh-n3ty35tq-4yhraefqf", 
	"history": [{...}], 
	"id": "34erfqqet24yghbrw6-46t24g4-45wvdafad-efge", 
	"kind": "task", 
	"status": { "state": "completed" } 
}
```

### AG-UI
AG-UI 通过开放协议标准化了前端应用与 AI Agent 的连接方式。可将其视为 AI 驱动系统的 通用翻译器，无论 Agent 使用哪种语言，AG-UI 都能确保流畅的沟通。AG-UI 帮助开发人员 构建需要实时交互、实时状态流和人机协作的下一代 AI 工作流程。明确且专注于 Agent 与 用户交互层。


## 三、 AG-UI 协议详解
>社区已经有 AG-UI 相关的项并具备定的共识：https://docs.ag-ui.com/introduction
![[Pasted image 20250629113101.png]]

注： AG-UI Client：通用的通信客户端（如 HttpAgent）或用于连接现有协议的专用客户端。

### 3.1. Events
事件是 Agent 和前端之间通信的基本单元，可实现实时、结构化的交互。

### 3.2. Agents
Agents 是 AG-UI 协议中处理请求并生成响应的核心组件。可以让前端应用而无需考虑底层 实现，能通过标准的接口与 AI 服务进行通信。


### 3.3. Message
消息遵循与供应商无关的格式，确保在保持一致结构的同时兼容不同的 LLM。使应用可以在 不同的模型厂商（如 OpenAI、Anthropic 或自定义模型）之间切换，而无需更改客户端实 现。
基本消息结构：

``` TYPESCRIPT
interface BaseMessage {
 id: string // 唯一的消息 ID
 role: string // 角色, system | user | assistant | tool
 content?: string // 内容（支持 Markdown）
 name?: string // 可选，发送消息的来源
 toolCalls: ToolCall[], // 可选，assistant 的工具调用配置
 toolCallId: string，// 可选，工具调用的结果
}

```
举例：
``` JAVASCRIPT
// 对话记录
[
 // 用户 query
 {
	 id: "msg_1",
	 role: "user",
	 content: "杭州今天天气如何?",
 },
 // LLM 返回工具调用消息
 {
	 id: "msg_2",
	 role: "assistant",
	 content: "让我为你查询今天杭州的天气",
	 toolCalls: [
	 {
		 id: "call_1",
		 type: "function",
		 function: {
			 name: "get_weather",
			 arguments: '{"location": "杭州", "unit": "摄氏度"}',
		 },
	 }],
 },
 // 工具调用结果给到 LLM
 {
	 id: "result_1",
	 role: "tool",
	 content: '{"temperature": 22, "condition": "晴天", "humidity": 25}',
	 toolCallId: "call_1",
 },
 // 大模型理解结果并返回
 {
	 id: "msg_3",
	 role: "assistant",
	 content: "杭州今天是晴天"
 }
```
### 3.4.State
状态管理可实现Agent与前端应⽤之间的实时同步、状态共享和更新机制。

### 3.5. Tools
⼯具，即前端应⽤的 UI 组件，动作等

``` TYPESCRIPT
 interface Tool {
	name: string // 工具名称
	description: string // 对工具的自然语言描述
	parameters: {
		 // 工具的参数
		type: "object"
		 properties: {
			// 具体的参数配置
		}
		 required: string[] // 必填的参数
	}
 }
```

举例：
``` javascript
// 前端工具定义
const userConfirmationTool = {
 name: "confirmAction",
 description: "Ask the user to confirm a specific action before proceeding",
	 parameters: {
		type: "object",
		 properties: {
		 action: {
			 type: "string",
			 description: "The action that needs user confirmation",
		 },
		 importance: {
			 type: "string",
			 enum: ["low", "medium", "high", "critical"],
			 description: "The importance level of the action",
		 },
	 },
	 required: ["action"],
	 },
 }

```

### 3.6. 前端研发定义⼀个AG-UI⼯具

>先以微应⽤研发阶段为主，介绍在微应⽤开发过程中，前端研发⼈员如何定义并创建出⼀ 个 AG-UI ⼯具。

微应⽤下的 AG-UI ⼯具的产出，将遵循微应⽤标准化⽅案。在微应⽤ hulu.manifest 定 义⽂件下，添加 AG-UI ⼯具的定义（需升级 @ali/hulu-utils>=1.14.0，提供类型⽀持）。下 ⾯是⼀个基本的示例：

``` typescript
// src/hulu.manifest.ts 
import { AgUITool } from '@ali/mlops-ag-ui'; 
import type { HuluManifest } from '@ali/hulu-utils';
const manifest: HuluManifest = {
	/** * 微应用下的页面路由路径 */
	routes: [],
	// ...other manifest options 
	/** * 微应用下导出的给风控智能体使用的 AG-UI 工具 */
	agUITools: [ 
		AgUITool.define( 
			// AG-UI 工具标识名。大驼峰写法 
			'<name>', 
			// AG-UI 工具配置项
			{ 
				// AG-UI 工具的描述。将提供给大模型分析使用 
				description: '简短描述当前 AG-UI 工具的功能、使用场景',
				// AG-UI 工具源码路径 
				source: './src/components/SomeComponent/index.tsx', 
				// AG-UI 工具的显示位置
				// - `analysis`: 显示在回复区下的分析结果区域
				// - `deep-analysis`: 显示在深度分析结果区域
				position: 'deep-analysis',
			},
		),
	],
};
```

### 3.7. 接⼝设计

1. 获取历史会话记录：
``` javascript
GET: /session/list
-- 
Response data:

[ 
	{ 
		"sessionId": "392cb1c7-9751-4198-82fc-6b1b0d7dd3fe", 
		"title": "你好", 
		"gmtModified": "2024-06-12T04:02:10.000+00:00" 
	} 
]
```

2. 根据会话 id 获取对话记录：
``` javascript
GET: /chat/findById--
query: sessionId=392cb1c7-9751-4198-82fc-6b1b0d7dd3fe--
Response
 data: [
	 {
		 queryId: 5571,
		 query: "你好",
		 sessionId:  "392cb1c7-9751-4198-82fc-6b1b0d7dd3fe",
		 response: {
			 "id": 5479,
			 "response": "很抱歉，这个问题暂时无法回答～换个问题试试看",
			 "ossUrlList": []
		 }
	 }
 ]
```

3. 创建会话
``` javascript
ST: /session/create--
Params:
 {
 "title": "你好"
 }--
Response:
 data: "c6eaf4ff-7639-4ba5-8ac0-e65b86cbb165"
```
4. 对话流
``` javascript
SSE POST: chat/query--
Params:
 {
	 "sessionId": "c6eaf4ff-7639-4ba5-8ac0-e65b86cbb165",
	 "query": "你好",
	 "ossUrlList": []
 }
 
 --
Response SSE:
 {"content":"我目前无法","id":2994181}
 {"content":"我目前无法解决你的问题","id":2994181}
```

## 四、智能体交互协议应⽤
> 社区有个开源项⽬ CopilotKit 提出 CoAgents：⼀个重塑⼈机交互 AI Agent 的前端框架， 通过 Agent UI 和 LangGraph 集成构建下⼀代交互式应⽤程序

### 4.1. 中台智能体 4.1.1. 对话流程

方式1：
![[Pasted image 20250629162924.png]]

方式2：
![[Pasted image 20250629163352.png]]
### 4.1.2. 对话内容协议
统⼀使⽤ LLM 的 MarkDown 格式返回，返回内容携带 Tag 区分 UI Tools：
``` JAVASCRIPT
{
	id":2994181,
	 "content":"
			 ## 这是你的预审列表
			```ui-tool
			 {
				 "componentType": "PRE_LIST",
				 "props": {
					 "taskId": "123",
					 ...
				 }
			}
```ui-tool
 {
 "componentType": "PRE_LIST_DETAIL",
 "props": {
 "taskId": "123",
 ...
 }
 }
 ```
```

```

### 4.1.3.注册 UI 组件协议


``` JAVASCIRIPT
[ {
 "componentType": "PRE_LIST", 
 "description": '简短描述当前 AgentUI 工具的功能、使用场景, 将提供给大模 型分析使用', "propTypes":
  {
   // "UI 工具需要的参数"
    "name": { // 参数名称 "describe": "",
     // 参数的描述，取值要求等
      "type": "string"
	       // 参数类型,string/int/boolean... 
	       },
        },
     required: ['name']      
} ]

```

组件定义可以通过⽂档上传到 RAG 知识库，在模型使⽤时通过上下⽂进⾏交互：

```
##角色
你是一位前端技术专家，专注于UI组件的应用与解析。
##技能
1. **
 ##限制
2. **
复制代码
组件注册**：根据${components}注册的组件解析组件用途和参数，为后续工作提供
可选的组件列表。
3. **
组件解析**：根据用户问题，你能够返回可用的UI组件；以及根据上一步获取到的答
案 `${result}` 理解其含义，分析出UI组件所需的合适参数。你必须确保参数类型、必
填项的正确性。
输出格式**：你的回答必须只有JSON格式：`{componentName: "组件名称", 
props: {...参数对象...}}`。不要回复除 JSON 格式之外的内容。
4. **
缺省组件**：当没有合适的UI组件匹配用户的需求时，你应返回默认组件
`{componentName: "Text", props: {"value": `${result}`}}`。
请确保所有回答都严格遵守上述规定
```
返回内容 使⽤标准 JSON 结构，由后端编排 LLM + MCP + UI Tools 的⼯具流程，将 MarkDown 内容 与 UI Tools 内容分别返回：

```
{
	 id":2994181,
	 "content":"这是你的10条预审列表",
	 "uiTools":[
		 {
			 "componentType": "PRE_LIST",
			 "props": {
				 "taskId": "123",
				 ...
			 }
		 }
	 ]
 }
```

## 5. 总结

给我的感觉就是，不要限制自己的边界，不要限制想象力，不要闭门造车。在跟着大模型发展的脚步走，就像玉伯大佬说的：大模型就是碾压机，它碾过的地方将一片坦途，我们要紧跟它的脚步，不能走在它前面。